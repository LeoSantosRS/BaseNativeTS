import * as React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { styles } from './styles';

export const Clients = () => {
    return (
        <View style={styles.container}>
          <Text>Pagina de Clientes</Text>
        </View> 
    );
}

